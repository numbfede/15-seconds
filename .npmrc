# Hosts like Render set NODE_ENV=production, which makes npm omit devDependencies.
# The build needs TypeScript and @types from there, and npm's `include` beats `omit`,
# so this keeps `npm install` working without a special build command.
include=dev
