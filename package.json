{
  "name": "15-seconds",
  "version": "1.0.0",
  "private": true,
  "description": "15 SECONDS — multiplayer HTML5 echo party game",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "npm run build -w shared && concurrently -n shared,server,client -c cyan,magenta,green \"npm run dev -w shared\" \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w client",
    "typecheck": "npm run build -w shared && npm run typecheck -w shared && npm run typecheck -w server && npm run typecheck -w client",
    "start": "npm run start -w server",
    "build:shared": "npm run build -w shared",
    "build:server": "npm run build -w shared && npm run build -w server",
    "build:client": "npm run build -w shared && npm run build -w client",
    "smoke": "node scripts/smoke-test.mjs"
  },
  "devDependencies": {
    "@railway/cli": "^5.45.2",
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20"
  }
}
